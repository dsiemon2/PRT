APP_NAME="Pecos River Traders Admin"
APP_ENV=local
APP_KEY=base64:mefCPCLmXFXYuU6eZp4kZtr6opOok0TIgp2rKUQAyRc=
APP_DEBUG=true
APP_URL=http://localhost:8301

# API URL for PHP requests (inside container)
# Use host.docker.internal to reach host machine from inside Docker container
API_BASE_URL=http://host.docker.internal:8300/api/v1

# API URL for JavaScript requests (browser-facing)
# Use localhost since browsers can't resolve host.docker.internal
API_PUBLIC_URL=http://localhost:8300/api/v1

# Storefront URL for images/assets (browser-facing)
STOREFRONT_URL=http://localhost:8300

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Docker MySQL Connection
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=pecosriver
DB_USERNAME=root
DB_PASSWORD=secret

SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="admin@pecosrivertrading.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
